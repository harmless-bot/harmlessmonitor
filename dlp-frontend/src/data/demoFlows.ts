import type { Flow, FlowHistoryItem, SystemStats } from '../types';

const now = Date.now();
const minutesAgo = (minutes: number) => new Date(now - minutes * 60_000).toISOString();

export const demoFlows: Flow[] = [
  {
    flow_id: 'demo-telegram-archive-upload',
    timestamp_start: minutesAgo(3),
    timestamp_end: minutesAgo(2),
    src_ip: '10.42.0.23',
    src_port: 52144,
    dst_ip: '149.154.167.220',
    dst_port: 443,
    protocol: 'TLS',
    resolved_site: 'Telegram API upload endpoint',
    domain: 'api.telegram.org',
    service_label: 'Telegram',
    organization: 'Telegram Messenger LLP',
    asn: 'AS62041',
    country: 'Netherlands',
    region: 'North Holland',
    lat: 52.37,
    lon: 4.9,
    geo_confidence: 'approximate_geo',
    confidence_score: 86,
    resolution_source: 'TLS SNI + ASN + GeoIP',
    bytes_out: 782_400_000,
    bytes_in: 4_200_000,
    packet_count: 9244,
    process_name: '7z-helper',
    first_seen: true,
    last_seen: minutesAgo(2),
    risk_score: 96,
    risk_level: 'critical',
    flags: ['archive_before_upload', 'large_outbound_transfer', 'first_seen_destination', 'cloud_upload'],
    why_flagged: [
      'Archive/compression activity followed by a large encrypted upload',
      'First-seen Telegram destination for this host',
      'Outbound volume is 186× larger than inbound traffic'
    ],
    strongest_indicators: ['archive → upload burst', 'first-seen service', 'non-browser TLS', 'large egress'],
    false_positive_possibilities: ['User-backed Telegram file transfer', 'Approved engineering artifact sharing'],
    recommended_steps: 'Inspect the source process tree, collect the archive filename, confirm user intent, and preserve TLS/SNI metadata.',
    detection_reason: 'Large outbound transfer to a first-seen cloud messaging endpoint from a non-browser compression process.',
    allowlist_status: 'unreviewed',
    simulated: true,
    is_sensitive: true,
    timestamp_ms: now - 3 * 60_000,
    matches: [{ pattern_name: 'Archive Upload Burst', severity: 10, category: 'exfiltration', snippet: '7z process opened finance_exports.zip before TLS upload' }]
  },
  {
    flow_id: 'demo-google-drive-high',
    timestamp_start: minutesAgo(9),
    timestamp_end: minutesAgo(6),
    src_ip: '10.42.0.18',
    src_port: 50082,
    dst_ip: '142.250.190.138',
    dst_port: 443,
    protocol: 'HTTPS',
    resolved_site: 'Google Drive upload',
    domain: 'www.googleapis.com',
    service_label: 'Google APIs',
    organization: 'Google LLC',
    asn: 'AS15169',
    country: 'United States',
    region: 'California',
    lat: 37.42,
    lon: -122.08,
    geo_confidence: 'cdn_anycast',
    confidence_score: 74,
    resolution_source: 'HTTP Host + ASN + CDN anycast',
    bytes_out: 248_500_000,
    bytes_in: 12_500_000,
    packet_count: 3812,
    process_name: 'python',
    first_seen: false,
    last_seen: minutesAgo(6),
    risk_score: 82,
    risk_level: 'high',
    flags: ['upload_heavy', 'cloud_storage', 'non_browser_process'],
    why_flagged: [
      'Script process uploaded a large volume to cloud storage',
      'Outbound traffic heavily exceeds normal host baseline'
    ],
    strongest_indicators: ['non-browser process', 'cloud storage upload', 'high egress ratio'],
    false_positive_possibilities: ['Automation job writing to an approved Workspace folder'],
    recommended_steps: 'Verify the script owner, destination Drive folder, OAuth client, and recent file access history.',
    detection_reason: 'Upload-heavy script session to Google APIs.',
    allowlist_status: 'unreviewed',
    simulated: true,
    is_sensitive: true,
    timestamp_ms: now - 9 * 60_000,
    matches: [{ pattern_name: 'Cloud Storage Upload', severity: 8, category: 'exfiltration', snippet: 'python sent sustained PUT/POST traffic to www.googleapis.com' }]
  },
  {
    flow_id: 'demo-github-medium',
    timestamp_start: minutesAgo(14),
    src_ip: '10.42.0.31',
    src_port: 49210,
    dst_ip: '140.82.114.4',
    dst_port: 443,
    protocol: 'HTTPS',
    resolved_site: 'GitHub API',
    domain: 'github.com',
    service_label: 'GitHub',
    organization: 'GitHub, Inc.',
    asn: 'AS36459',
    country: 'United States',
    region: 'Virginia',
    lat: 39.04,
    lon: -77.49,
    geo_confidence: 'approximate_geo',
    confidence_score: 89,
    resolution_source: 'DNS cache + TLS SNI + ASN',
    bytes_out: 38_700_000,
    bytes_in: 55_300_000,
    packet_count: 1544,
    process_name: 'git',
    first_seen: false,
    last_seen: minutesAgo(13),
    risk_score: 48,
    risk_level: 'medium',
    flags: ['developer_tool', 'moderate_egress'],
    why_flagged: ['Developer tool transferred a moderate amount of data to a code hosting service'],
    strongest_indicators: ['code hosting', 'moderate egress'],
    false_positive_possibilities: ['Normal repository push or release upload'],
    recommended_steps: 'Confirm repository owner and recent push events before escalating.',
    detection_reason: 'Moderate data egress to a code hosting provider.',
    allowlist_status: 'unreviewed',
    simulated: true,
    timestamp_ms: now - 14 * 60_000
  },
  {
    flow_id: 'demo-cloudflare-low',
    timestamp_start: minutesAgo(18),
    src_ip: '10.42.0.12',
    src_port: 49944,
    dst_ip: '104.18.32.47',
    dst_port: 443,
    protocol: 'HTTPS',
    resolved_site: 'Cloudflare-proxied SaaS',
    domain: 'cdn.example-saas.com',
    service_label: 'Cloudflare CDN',
    organization: 'Cloudflare, Inc.',
    asn: 'AS13335',
    country: 'ASN-level only',
    region: 'Global Anycast',
    geo_confidence: 'cdn_anycast',
    confidence_score: 58,
    resolution_source: 'TLS SNI + ASN; map suppressed because CDN anycast is imprecise',
    bytes_out: 7_900_000,
    bytes_in: 31_100_000,
    packet_count: 864,
    process_name: 'Chrome',
    first_seen: false,
    last_seen: minutesAgo(17),
    risk_score: 19,
    risk_level: 'low',
    flags: ['cdn_anycast', 'browser_tls'],
    why_flagged: ['Known CDN destination with low outbound volume'],
    strongest_indicators: ['known CDN', 'browser process'],
    false_positive_possibilities: ['Normal SaaS activity'],
    recommended_steps: 'No action needed unless this service is not expected for the host.',
    detection_reason: 'Low-risk browser traffic through a CDN.',
    allowlist_status: 'unreviewed',
    simulated: true,
    timestamp_ms: now - 18 * 60_000
  },
  {
    flow_id: 'demo-aws-unresolved-high',
    timestamp_start: minutesAgo(21),
    src_ip: '10.42.0.44',
    src_port: 53421,
    dst_ip: '18.214.12.91',
    dst_port: 8443,
    protocol: 'TLS',
    resolved_site: 'Partially resolved AWS endpoint',
    domain: 'unresolved-sni',
    service_label: 'AWS EC2',
    organization: 'Amazon Web Services',
    asn: 'AS14618',
    country: 'United States',
    region: 'Virginia',
    lat: 39.04,
    lon: -77.49,
    geo_confidence: 'asn_region',
    confidence_score: 52,
    resolution_source: 'ASN lookup only',
    unresolved_reason: 'Encrypted session did not expose DNS or TLS SNI; identity limited to ASN/provider.',
    bytes_out: 132_000_000,
    bytes_in: 2_100_000,
    packet_count: 2110,
    process_name: 'backupd',
    first_seen: true,
    last_seen: minutesAgo(20),
    risk_score: 78,
    risk_level: 'high',
    flags: ['first_seen_destination', 'upload_heavy', 'unresolved_encrypted', 'unusual_port'],
    why_flagged: [
      'First-seen AWS endpoint with no exposed DNS/SNI',
      'Encrypted upload-heavy session on non-standard port 8443'
    ],
    strongest_indicators: ['first-seen', 'unresolved encrypted', 'unusual port', 'large egress'],
    false_positive_possibilities: ['New backup target or approved cloud workload'],
    recommended_steps: 'Validate backup configuration, check change tickets, and capture process command line.',
    detection_reason: 'Repeated unresolved encrypted upload session to a cloud provider.',
    allowlist_status: 'unreviewed',
    simulated: true,
    is_sensitive: true,
    timestamp_ms: now - 21 * 60_000,
    matches: [{ pattern_name: 'Unresolved Encrypted Upload', severity: 8, category: 'exfiltration', snippet: 'TLS on 8443 to AWS ASN without SNI' }]
  },
  {
    flow_id: 'demo-private-local',
    timestamp_start: minutesAgo(26),
    src_ip: '10.42.0.9',
    src_port: 50120,
    dst_ip: '192.168.1.20',
    dst_port: 445,
    protocol: 'SMB',
    resolved_site: 'Internal file server',
    domain: 'fileserver.local',
    service_label: 'Internal SMB',
    organization: 'Private RFC1918 network',
    country: 'Internal',
    region: 'Local network',
    geo_confidence: 'private_local',
    confidence_score: 100,
    resolution_source: 'Local DNS cache',
    bytes_out: 2_400_000,
    bytes_in: 91_000_000,
    packet_count: 670,
    process_name: 'Finder',
    first_seen: false,
    last_seen: minutesAgo(25),
    risk_score: 8,
    risk_level: 'low',
    flags: ['private_local'],
    why_flagged: ['Private local destination; no external exfil signal'],
    strongest_indicators: ['internal file server'],
    false_positive_possibilities: ['Normal file browsing'],
    recommended_steps: 'No investigation needed unless SMB access is unexpected.',
    detection_reason: 'Benign local file server traffic.',
    allowlist_status: 'unreviewed',
    simulated: true,
    timestamp_ms: now - 26 * 60_000
  }
];

export const demoHistory: FlowHistoryItem[] = demoFlows.map((flow) => ({
  flow_id: flow.flow_id,
  timestamp_ms: flow.timestamp_ms ?? Date.parse(flow.timestamp_start),
  src: flow.src_ip,
  dst: flow.dst_ip,
  bandwidth_bps: Math.round((flow.bytes_out + flow.bytes_in) / 60),
  sensitive_count: flow.is_sensitive ? 1 : 0,
  protocol: flow.protocol,
  is_threat: ['medium', 'high', 'critical'].includes(flow.risk_level)
}));

export const demoStats: SystemStats = {
  packets_total: demoFlows.reduce((total, flow) => total + flow.packet_count, 0),
  sensitive_total: demoFlows.filter((flow) => flow.is_sensitive).length,
  fps: 128,
  latency_ms: 42,
  flows: demoHistory.map((item) => ({
    flow_id: item.flow_id,
    src: item.src,
    dst: item.dst,
    bandwidth_bps: item.bandwidth_bps,
    sensitive_count: item.sensitive_count
  }))
};
